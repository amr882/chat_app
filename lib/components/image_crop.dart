import 'package:flutter/material.dart';

class ImageCrop extends StatelessWidget {
  const ImageCrop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("this is photo")));
  }
}
