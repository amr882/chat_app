import 'package:flutter/material.dart';

class CallHestory extends StatelessWidget {
  const CallHestory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.blueAccent);
  }
}
